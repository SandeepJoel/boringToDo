import React from 'react';
export const ColorLiquids = (props) => {
  let { type, config } = props.settings;
  return (
    <div className="effect-container">
      <div class="svg-list">
      </div>
    </div>
  );
};